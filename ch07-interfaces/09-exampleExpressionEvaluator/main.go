package main

import (
	"fmt"
)

func main() {
	fmt.Println("Example: Expression Evaluator")

	// Example Expressions:
	// sqrt(A / pi)
	// pow(x, 3) + pow(y, 3)
	// (F - 32) * 5 / 9

	//TODO: Continue when passed chapter 10 Tests and chapter 13 for the parser
	// from comment from ch7/eval
	// This lexer is similar to the one described in Chapter 13.
}
